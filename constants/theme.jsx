import { Utils } from "expo-ui-kit";

const theme ={

     COLORS:{
        primary:"#0094FC",
     },
        SIZES:{ 
     
        },
        };

export default Utils.mergeTheme(Utils.theme, theme);